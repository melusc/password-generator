{
	"devDependencies": {
		"@lusc/eslint-config": "^12.0.0",
		"@lusc/tsconfig": "^7.2.1",
		"@types/node": "^24.13.2",
		"eslint": "^10.5.0",
		"prettier": "^3.8.4",
		"rimraf": "^6.1.3",
		"typescript": "^6.0.3"
	},
	"engines": {
		"node": "^24.18.0"
	},
	"private": true,
	"scripts": {
		"build": "rimraf dist && tsc",
		"start": "node dist/src/cli.js",
		"dev": "node --run build && node --run start",
		"fmt": "prettier -w . && eslint --fix",
		"test": "node --run build && node --test dist/test/*.js"
	},
	"type": "module",
	"packageManager": "pnpm@11.9.0",
	"dependencies": {
		"@lusc/util": "^2.1.1"
	}
}
